// Define shared enums and potentially interfaces used across frontend and backend

export enum AppointmentStatus {
  SCHEDULED = "scheduled",
  CONFIRMED = "confirmed",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
  NO_SHOW = "no_show",
  RESCHEDULED = "rescheduled",
}

// Add other shared interfaces or types if needed, e.g., for User roles
// export interface User { ... }
